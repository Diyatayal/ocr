services:
  - type: web
    name: streamlit-ocr-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.enableCORS false
    preDeployCommand: ./setup.sh
    envVars:
      - key: PORT
        value: 8000
